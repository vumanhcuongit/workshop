version: '2'
services:
  base:
    image: node:10
    volumes:
      - .:/usr/src/app/
    working_dir: /usr/src/app/
  install:
    extends:
      service: base
    command: yarn
  db:
    extends:
      service: base
    command: yarn
